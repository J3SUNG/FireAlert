/**
 * 산불 알림 UI 관련 타입 정의 공개 API
 *
 * 산불 알림 UI 컴포넌트에서 사용하는 타입과 인터페이스를 제공합니다.
 * 외부에서 필요한 타입만 선택적으로 내보냅니다.
 */

import type {
  FireAlertHeaderProps,
  FireAlertContentProps,
  FireAlertSidebarProps,
  ResponseLevelCounts,
  FireAlertFilterProps
} from './types';

// 타입 내보내기
export type {
  FireAlertHeaderProps,
  FireAlertContentProps,
  FireAlertSidebarProps,
  ResponseLevelCounts,
  FireAlertFilterProps
};
